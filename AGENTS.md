# AGENTS.md

이 문서는 이 저장소에서 에이전트가 코드를 작성하거나 테스트를 보강할 때 따르는 작업 기준을 정리한다.

## 핵심 원칙

테스트 대상 객체는 실제 객체로 실행하고, 테스트 대상이 의존하는 외부 협력 객체는 Mock으로 대체한다. 도메인 엔티티, DTO, 값 객체는 가능하면 실제 객체를 사용한다.

다만 모든 상황에 기계적으로 적용하지 않는다. 이번 테스트가 무엇을 증명하려는지에 따라 Mock과 실제 객체를 선택한다.

## 테스트 작성 원칙

1. 테스트 대상 Service는 실제 객체로 실행한다.
2. Repository, 다른 Service, Reader, 파일 저장소는 Mock으로 둔다.
3. Command, DTO, record, 단순 값은 실제 객체를 만든다.
4. Entity의 상태 변경이 관심사라면 실제 Entity를 만든다.
5. Entity가 단순 참조 또는 전달 역할만 한다면 Mock도 가능하다.
6. Mock Repository 테스트만으로 DB 저장을 검증했다고 판단하지 않는다.
7. Mapper를 Mock으로 사용했다면 실제 Mapper 테스트를 별도로 둔다.
8. JPA 쿼리는 `@DataJpaTest`에서 검증한다.
9. 트랜잭션 커밋, 롤백, 전파는 `@SpringBootTest` 통합 테스트에서 검증한다.
10. `InOrder`와 `verifyNoMoreInteractions`는 실제 요구사항일 때만 사용한다.

## Mock과 실제 객체 선택

- Mock은 협력 객체를 어떻게 사용했는지 검증하는 도구다.
- 실제 객체는 로직 실행 후 반환값과 상태가 실제로 올바른지 검증하는 도구다.
- 서비스 단위 테스트에서는 `@InjectMocks` 또는 생성자를 통해 테스트 대상 Service를 실제 객체로 실행한다.
- `@Mock`으로 둔 Repository, 다른 Service, Reader, Mapper, 파일 저장소는 실제 구현을 실행하지 않는다.
- Mock 객체의 메서드는 기본적으로 실제 로직을 실행하지 않는다. 반환값이 필요하면 `given(...).willReturn(...)`으로 명시한다.
- Mock은 호출 여부, 전달 인자, 호출 횟수, 호출 순서를 관찰할 때 사용한다.
- 상태 변경을 확인해야 하는 Entity는 Mock으로 만들지 말고 실제 객체를 만든다.
- 단순히 다른 객체에 전달되는 Entity나 조회 결과 목록은 Mock을 사용할 수 있다.
- DTO나 record는 생성이 어렵지 않다면 실제 객체를 사용한다.
- DTO의 특정 메서드 반환값 하나만 필요하고 생성 비용이 크다면 Mock을 사용할 수 있다.
- Spy는 실제 메서드 실행과 호출 검증이 모두 필요할 때만 제한적으로 사용한다. 상태 검증만으로 충분하면 Spy를 쓰지 않는다.

## 서비스 단위 테스트 기준

- 서비스 단위 테스트는 서비스의 분기, 예외, 협력 객체 호출 계약을 검증한다.
- 서비스가 생성한 Entity는 `ArgumentCaptor`로 캡처해서 실제 상태를 확인할 수 있다.
- Mapper를 Mock으로 둔 경우 DTO 필드의 실제 매핑 결과를 서비스 테스트에서 과하게 검증하지 않는다.
- Repository를 Mock으로 둔 경우 영속성, 쿼리, 트랜잭션 동작을 검증하지 않는다.
- Mock Repository의 `save(...)`에 id를 넣어 반환하는 것은 JPA 저장을 흉내 내는 것일 뿐이다.
- Mock Repository 테스트로는 `INSERT`, 영속성 컨텍스트, DB 제약조건, 연관관계 매핑, cascade, 트랜잭션 커밋을 검증할 수 없다.
- 예외가 발생한 뒤 저장, 삭제, 파일 처리, 외부 호출이 진행되지 않아야 한다면 `never()` 또는 `verifyNoInteractions(...)`로 검증한다.

## 상호작용 검증 기준

- `InOrder`는 호출 순서가 바뀌면 기능이 잘못되는 경우에만 사용한다.
- 예를 들어 자식 데이터나 첨부 파일을 먼저 삭제한 뒤 부모 데이터를 삭제해야 하는 흐름은 `InOrder` 검증이 적절하다.
- 단순 조회 후 mapper 호출처럼 결과는 같고 구현 순서만 바뀔 수 있는 흐름에는 `InOrder`를 남용하지 않는다.
- `verifyNoMoreInteractions(...)`는 추가 호출 자체가 버그일 때만 사용한다.
- 실패 이후 저장이 실행되면 안 되는 경우, 외부 API가 중복 호출되면 안 되는 경우, N+1 방지가 테스트 목적일 때는 추가 호출 금지가 의미 있다.
- 단순 정상 흐름에서는 반환값과 핵심 호출만 검증하고 불필요하게 모든 호출을 고정하지 않는다.

## 테스트 종류별 책임

### 서비스 단위 테스트

- `@ExtendWith(MockitoExtension.class)`를 사용한다.
- Service는 실제 객체로 실행한다.
- Repository, 다른 Service, Reader, 외부 API, 파일 저장소는 Mock으로 둔다.
- Command, DTO, record, 값 객체는 가능하면 실제 객체로 둔다.
- Entity는 상태 변경을 검증하면 실제 객체, 단순 전달이면 Mock을 사용할 수 있다.
- 검증 대상은 분기, 예외, 협력 객체 호출, 전달 인자, 도메인 객체 생성, 반환값이다.

### 도메인 단위 테스트

- Entity와 값 객체를 실제 객체로 만든다.
- 상태 변경, 도메인 규칙, 불변식, 잘못된 상태에 대한 예외를 검증한다.
- 예를 들어 `Message.updateInfo(...)`가 content를 실제로 바꾸는지는 도메인 단위 테스트로도 검증할 수 있다.

### Mapper 테스트

- 서비스 테스트에서 Mapper를 Mock으로 사용했다면 실제 Mapper 테스트를 별도로 둔다.
- 실제 Mapper 테스트에서는 Entity와 DTO 필드 매핑, 시간 변환, 첨부 파일 변환을 검증한다.

### Repository 테스트

- `@DataJpaTest`를 사용한다.
- Repository, EntityManager, 테스트 DB를 실제로 사용한다.
- JPQL, Querydsl, Native SQL, 연관관계, fetch 전략, 정렬, 페이징, DB 저장을 검증한다.

### 통합 테스트

- `@SpringBootTest`를 사용한다.
- 실제 Spring Bean, Repository, Mapper, DB, 트랜잭션을 사용한다.
- 여러 Service 간 연동, 실제 저장과 재조회, 트랜잭션 커밋, 롤백, 전파를 검증한다.

## 테스트 작성 전 판단 질문

1. 지금 검증하는 대상은 누구인가?
2. 이 의존성의 실제 동작까지 검증해야 하는가?
3. 이 객체의 상태 변화를 확인해야 하는가?
4. 이 객체는 단순히 전달만 되는가?
5. 실제 DB, JPA, 트랜잭션 동작이 필요한가?
6. 호출 순서가 실제 비즈니스 요구사항인가?
7. 추가 호출이 발생하면 실제 버그인가?